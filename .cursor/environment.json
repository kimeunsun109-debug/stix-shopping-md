{
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "pip3 install --break-system-packages -r requirements.txt && python3 -m playwright install --with-deps chromium"
}
